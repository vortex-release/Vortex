foreach(pair "name;NAME" "id;ID" "channel;CHANNEL")
  list(GET pair 0 key)
  list(GET pair 1 suffix)
  string(JSON VORTEX_${suffix} GET "${VORTEX_RELEASE_CONFIG}" ${key})
endforeach()
string(JSON VORTEX_CONFIG_URL GET "${VORTEX_RELEASE_CONFIG}" updateUrl)
if(NOT DEFINED VORTEX_UPDATE_URL)
  set(VORTEX_UPDATE_URL "${VORTEX_CONFIG_URL}")
endif()
if(NOT VORTEX_UPDATE_URL STREQUAL "" AND NOT VORTEX_UPDATE_URL MATCHES "^https?://[A-Za-z0-9.-]+(:[0-9]+)?(/[A-Za-z0-9._/-]*)?$")
  message(FATAL_ERROR "Invalid update URL")
endif()
configure_file(app_config.hpp.in "${CMAKE_CURRENT_BINARY_DIR}/app_config.hpp" @ONLY)
target_include_directories(EntityAwarenessOverlay PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
set(VELOPACK_DIR "${PROJECT_SOURCE_DIR}/dependencies/velopack")
add_library(vortex_updater_sdk SHARED IMPORTED GLOBAL)
set_target_properties(vortex_updater_sdk PROPERTIES
  IMPORTED_IMPLIB "${VELOPACK_DIR}/lib/velopack_libc_win_x64_msvc.dll.lib"
  IMPORTED_LOCATION "${VELOPACK_DIR}/lib/velopack_libc_win_x64_msvc.dll"
  INTERFACE_INCLUDE_DIRECTORIES "${VELOPACK_DIR}/include")
add_library(vortex_core STATIC loader.cpp updater.cpp github_release.cpp)
target_include_directories(vortex_core PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}" "${CMAKE_CURRENT_BINARY_DIR}" "${PROJECT_SOURCE_DIR}/dependencies")
target_link_libraries(vortex_core PUBLIC vortex_updater_sdk shell32 ole32 winhttp bcrypt)
configure_file(vortex.rc.in "${CMAKE_CURRENT_BINARY_DIR}/vortex.rc" @ONLY)
add_executable(Vortex WIN32 main.cpp "${imgui_SOURCE_DIR}/backends/imgui_impl_win32.cpp" "${CMAKE_CURRENT_BINARY_DIR}/vortex.rc")
target_link_libraries(Vortex PRIVATE vortex_core awareness_imgui d3d11 dxgi user32 gdi32 dwmapi)
add_dependencies(Vortex EntityAwarenessOverlay ObserverDemo)
add_custom_command(TARGET Vortex POST_BUILD
  COMMAND "${CMAKE_COMMAND}" -E copy_if_different "$<TARGET_FILE:vortex_updater_sdk>" "$<TARGET_FILE_DIR:Vortex>/velopack_libc.dll"
  COMMAND "${CMAKE_COMMAND}" -E copy_if_different "$<TARGET_FILE:EntityAwarenessOverlay>" "$<TARGET_FILE_DIR:Vortex>"
  COMMAND "${CMAKE_COMMAND}" -E copy_if_different "$<TARGET_FILE:ObserverDemo>" "$<TARGET_FILE_DIR:Vortex>"
  COMMAND "${CMAKE_COMMAND}" -E copy_if_different "${PROJECT_SOURCE_DIR}/DefaultSettings.ini" "$<TARGET_FILE_DIR:Vortex>"
  COMMAND "${CMAKE_COMMAND}" -E copy_directory "${PROJECT_SOURCE_DIR}/profile" "$<TARGET_FILE_DIR:Vortex>/profile")
add_executable(VortexUpdateServer update_server.cpp)
target_link_libraries(VortexUpdateServer PRIVATE ws2_32 shell32 ole32)
foreach(target vortex_core Vortex VortexUpdateServer)
  target_compile_definitions(${target} PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)
  target_compile_options(${target} PRIVATE /W4 /permissive- /EHsc /utf-8)
endforeach()
if(BUILD_TESTING)
  add_library(VortexLoaderFixture SHARED ../tests/loader_fixture.cpp)
  add_executable(vortex_loader_tests ../tests/loader_tests.cpp loader.cpp)
  target_link_libraries(vortex_loader_tests PRIVATE shell32 ole32)
  target_compile_definitions(vortex_loader_tests PRIVATE UNICODE _UNICODE NOMINMAX WIN32_LEAN_AND_MEAN _WIN32_WINNT=0x0A00)
  add_dependencies(vortex_loader_tests VortexLoaderFixture)
  add_test(NAME vortex_loader COMMAND vortex_loader_tests)
  set_tests_properties(vortex_loader PROPERTIES TIMEOUT 45)
  add_test(NAME vortex_launcher COMMAND Vortex --smoke)
  set_tests_properties(vortex_launcher PROPERTIES TIMEOUT 30 ENVIRONMENT "VORTEX_DATA_DIR=${CMAKE_CURRENT_BINARY_DIR}/test-profile")
endif()

if(BUILD_TESTING)
  add_executable(vortex_profile_tests ../tests/profile_migration_tests.cpp ../src/settings.cpp)
  target_link_libraries(vortex_profile_tests PRIVATE awareness_math)
  target_include_directories(vortex_profile_tests PRIVATE ../src)
  target_compile_definitions(vortex_profile_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  target_compile_options(vortex_profile_tests PRIVATE /W4 /EHsc /utf-8)
  add_test(NAME vortex_profile_migration COMMAND vortex_profile_tests)
  set_tests_properties(vortex_profile_migration PROPERTIES TIMEOUT 20 ENVIRONMENT "VORTEX_DATA_DIR=${CMAKE_CURRENT_BINARY_DIR}/profile-test")
  find_program(VORTEX_POWERSHELL NAMES powershell pwsh REQUIRED)
  add_test(NAME vortex_update_server COMMAND "${VORTEX_POWERSHELL}" -NoProfile -ExecutionPolicy Bypass -File "${PROJECT_SOURCE_DIR}/tests/update_server_tests.ps1" -Server "$<TARGET_FILE:VortexUpdateServer>" -TestDirectory "${CMAKE_CURRENT_BINARY_DIR}/server-tests")
  set_tests_properties(vortex_update_server PROPERTIES TIMEOUT 20)
endif()
if(BUILD_TESTING)
  add_executable(vortex_github_update_tests ../tests/github_update_tests.cpp)
  target_link_libraries(vortex_github_update_tests PRIVATE vortex_core)
  target_compile_definitions(vortex_github_update_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  add_custom_command(TARGET vortex_github_update_tests POST_BUILD COMMAND "${CMAKE_COMMAND}" -E copy_if_different "$<TARGET_FILE:vortex_updater_sdk>" "$<TARGET_FILE_DIR:vortex_github_update_tests>/velopack_libc.dll")
  add_test(NAME vortex_github_updates COMMAND vortex_github_update_tests)
  set_tests_properties(vortex_github_updates PROPERTIES TIMEOUT 30 ENVIRONMENT "VORTEX_DATA_DIR=${CMAKE_CURRENT_BINARY_DIR}/github-test")
endif()

if(BUILD_TESTING)
  add_test(NAME vortex_release_support COMMAND "${VORTEX_POWERSHELL}" -NoProfile -ExecutionPolicy Bypass -File "${PROJECT_SOURCE_DIR}/tests/release_support_tests.ps1" -TestDirectory "${CMAKE_CURRENT_BINARY_DIR}/release-support-test")
  set_tests_properties(vortex_release_support PROPERTIES TIMEOUT 20)
endif()
