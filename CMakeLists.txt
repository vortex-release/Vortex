cmake_minimum_required(VERSION 3.24)
file(READ "${CMAKE_CURRENT_SOURCE_DIR}/release/config.json" VORTEX_RELEASE_CONFIG)
string(JSON VORTEX_RELEASE_VERSION GET "${VORTEX_RELEASE_CONFIG}" version)
if(NOT VORTEX_RELEASE_VERSION MATCHES "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$")
  message(FATAL_ERROR "release/config.json must contain a stable semantic version")
endif()
if(VORTEX_VERSION_OVERRIDE AND NOT VORTEX_VERSION_OVERRIDE STREQUAL VORTEX_RELEASE_VERSION)
  message(FATAL_ERROR "Update the central version in release/config.json instead of overriding it")
endif()
project(EntityAwarenessOverlay VERSION ${VORTEX_RELEASE_VERSION} LANGUAGES C CXX)
# Embed the MSVC runtime: the release needs no extra Visual C++ runtime DLLs.
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
include(CTest)
add_library(awareness_math INTERFACE)
target_include_directories(awareness_math INTERFACE include)
if(BUILD_TESTING)
  add_executable(awareness_math_tests tests/math_tests.cpp)
  target_link_libraries(awareness_math_tests PRIVATE awareness_math)
  add_test(NAME awareness_math COMMAND awareness_math_tests)
  add_executable(awareness_pattern_tests tests/pattern_tests.cpp)
  target_link_libraries(awareness_pattern_tests PRIVATE awareness_math)
  add_test(NAME awareness_patterns COMMAND awareness_pattern_tests)
  add_executable(awareness_cs2_reader_tests tests/cs2_reader_tests.cpp)
  target_link_libraries(awareness_cs2_reader_tests PRIVATE awareness_math)
  target_include_directories(awareness_cs2_reader_tests PRIVATE src)
  target_compile_definitions(awareness_cs2_reader_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  add_test(NAME awareness_cs2_reader COMMAND awareness_cs2_reader_tests)
  add_executable(awareness_camera_tracking_tests tests/camera_tracking_tests.cpp)
  target_link_libraries(awareness_camera_tracking_tests PRIVATE awareness_math)
  add_test(NAME awareness_camera_tracking COMMAND awareness_camera_tracking_tests)
  add_executable(awareness_fov_ring_tests tests/fov_ring_tests.cpp)
  target_link_libraries(awareness_fov_ring_tests PRIVATE awareness_math)
  add_test(NAME awareness_fov_ring COMMAND awareness_fov_ring_tests)
  add_executable(awareness_ring_tests tests/awareness_ring_tests.cpp)
  target_link_libraries(awareness_ring_tests PRIVATE awareness_math)
  add_test(NAME awareness_ring COMMAND awareness_ring_tests)
endif()
if(NOT WIN32)
  message(STATUS "Only the portable math, pattern, reader, and camera tests are built outside Windows.")
  return()
endif()
if(NOT CMAKE_SIZEOF_VOID_P EQUAL 8)
  message(FATAL_ERROR "Build the Windows DLL and demo for x64.")
endif()
target_link_libraries(awareness_math INTERFACE shell32 ole32)
include(FetchContent)
foreach(dependency imgui minhook)
  string(TOUPPER "${dependency}" dependency_upper)
  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/dependencies/${dependency}-src" AND NOT FETCHCONTENT_SOURCE_DIR_${dependency_upper})
    set(FETCHCONTENT_SOURCE_DIR_${dependency_upper} "${CMAKE_CURRENT_SOURCE_DIR}/dependencies/${dependency}-src" CACHE PATH "Bundled dependency source")
  endif()
endforeach()
FetchContent_Declare(imgui
  GIT_REPOSITORY https://github.com/ocornut/imgui.git
  GIT_TAG v1.92.9
  GIT_SHALLOW TRUE)
FetchContent_Declare(minhook
  GIT_REPOSITORY https://github.com/TsudaKageyu/minhook.git
  GIT_TAG v1.3.4
  GIT_SHALLOW TRUE)
set(BUILD_SHARED_LIBS OFF CACHE BOOL "Build dependencies statically" FORCE)
FetchContent_MakeAvailable(imgui minhook)
add_library(awareness_imgui STATIC
  ${imgui_SOURCE_DIR}/imgui.cpp
  ${imgui_SOURCE_DIR}/imgui_draw.cpp
  ${imgui_SOURCE_DIR}/imgui_tables.cpp
  ${imgui_SOURCE_DIR}/imgui_widgets.cpp
  ${imgui_SOURCE_DIR}/backends/imgui_impl_dx11.cpp)
target_include_directories(awareness_imgui PUBLIC ${imgui_SOURCE_DIR} ${imgui_SOURCE_DIR}/backends)
target_link_libraries(awareness_imgui PRIVATE d3d11 d3dcompiler)
enable_language(RC)
if(MSVC)
  enable_language(ASM_MASM)
endif()
configure_file(src/overlay_resources.rc.in overlay_resources.rc @ONLY)
add_library(EntityAwarenessOverlay SHARED src/dllmain.cpp src/overlay.cpp src/grenade_lineups.cpp src/steam_profile.cpp src/trajectory_native.cpp src/trajectory_gpu.cpp src/scene_depth_capture.cpp src/native_model_mask.cpp src/native_draw_shim.asm src/depth_resolve.cpp src/area_renderer.cpp src/world_native.cpp src/viewmodel_native.cpp src/game_frame.cpp src/scoreboard.cpp src/scoreboard_native.cpp src/weather_native.cpp src/cosmetics_native.cpp src/economy_catalog.cpp src/kill_sound.cpp src/cs2_model_fill.cpp src/model_preview.cpp src/entity_effects.cpp src/address_discovery.cpp src/cs2_source.cpp src/window_bridge.cpp src/settings.cpp "${CMAKE_CURRENT_BINARY_DIR}/overlay_resources.rc")
target_compile_definitions(EntityAwarenessOverlay PRIVATE AWARENESS_EXPORTS)
target_link_libraries(EntityAwarenessOverlay PRIVATE awareness_math awareness_imgui minhook d3d11 d3dcompiler dxgi user32 psapi windowscodecs ole32 shell32 comdlg32 mfplat mfreadwrite mfuuid xaudio2 winmm)
target_include_directories(EntityAwarenessOverlay PRIVATE src)
add_executable(ObserverDemo WIN32 demo/main.cpp src/window_bridge.cpp)
target_include_directories(ObserverDemo PRIVATE src)
target_link_libraries(ObserverDemo PRIVATE awareness_math awareness_imgui d3d11 dxgi user32 gdi32)
add_dependencies(ObserverDemo EntityAwarenessOverlay)
add_subdirectory(app)
if(BUILD_TESTING AND MSVC)
  add_executable(awareness_trajectory_gpu_tests tests/trajectory_gpu_tests.cpp src/trajectory_gpu.cpp src/scene_depth_capture.cpp src/native_model_mask.cpp src/native_draw_shim.asm src/depth_resolve.cpp src/area_renderer.cpp src/entity_effects.cpp)
  target_include_directories(awareness_trajectory_gpu_tests PRIVATE src)
  target_link_libraries(awareness_trajectory_gpu_tests PRIVATE awareness_math minhook d3d11 d3dcompiler)
  target_compile_definitions(awareness_trajectory_gpu_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_trajectory_gpu_tests PRIVATE "$<$<COMPILE_LANGUAGE:CXX>:/W4;/permissive-;/EHsc;/utf-8>")
  add_test(NAME awareness_trajectory_gpu COMMAND awareness_trajectory_gpu_tests)
  add_library(SteamProfileFixture SHARED tests/steam_api_fixture.cpp)
  set_target_properties(SteamProfileFixture PROPERTIES OUTPUT_NAME steam_api64)
  add_executable(awareness_scene_grade_tests tests/scene_grade_tests.cpp)
  target_include_directories(awareness_scene_grade_tests PRIVATE src)
  target_link_libraries(awareness_scene_grade_tests PRIVATE awareness_math d3d11 d3dcompiler)
  target_compile_definitions(awareness_scene_grade_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_scene_grade_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_scene_grade COMMAND awareness_scene_grade_tests)
  add_executable(awareness_steam_profile_tests tests/steam_profile_tests.cpp src/steam_profile.cpp)
  target_include_directories(awareness_steam_profile_tests PRIVATE src)
  target_link_libraries(awareness_steam_profile_tests PRIVATE awareness_math awareness_imgui d3d11 windowscodecs ole32)
  target_compile_definitions(awareness_steam_profile_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_steam_profile_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_dependencies(awareness_steam_profile_tests SteamProfileFixture)
  add_dependencies(ObserverDemo SteamProfileFixture)
  add_test(NAME awareness_steam_profile COMMAND awareness_steam_profile_tests)
  add_executable(awareness_menu_motion_tests tests/menu_motion_tests.cpp)
  target_include_directories(awareness_menu_motion_tests PRIVATE src)
  add_test(NAME awareness_menu_motion COMMAND awareness_menu_motion_tests)
  add_executable(awareness_combat_tests tests/combat_tests.cpp)
  target_link_libraries(awareness_combat_tests PRIVATE awareness_math awareness_imgui)
  target_include_directories(awareness_combat_tests PRIVATE src)
  target_compile_definitions(awareness_combat_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  add_test(NAME awareness_combat COMMAND awareness_combat_tests)
  add_executable(awareness_trajectory_tests tests/trajectory_tests.cpp)
  target_link_libraries(awareness_trajectory_tests PRIVATE awareness_math awareness_imgui)
  target_include_directories(awareness_trajectory_tests PRIVATE src)
  target_compile_definitions(awareness_trajectory_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_trajectory_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_trajectories COMMAND awareness_trajectory_tests)
  add_executable(awareness_kill_events_tests tests/kill_events_tests.cpp)
  target_link_libraries(awareness_kill_events_tests PRIVATE awareness_math)
  target_include_directories(awareness_kill_events_tests PRIVATE src)
  target_compile_definitions(awareness_kill_events_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  add_test(NAME awareness_kill_events COMMAND awareness_kill_events_tests)
  add_executable(awareness_kill_sound_tests tests/kill_sound_tests.cpp src/kill_sound.cpp)
  target_link_libraries(awareness_kill_sound_tests PRIVATE awareness_math mfplat mfreadwrite mfuuid xaudio2 winmm ole32 comdlg32)
  target_include_directories(awareness_kill_sound_tests PRIVATE src)
  target_compile_definitions(awareness_kill_sound_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  target_compile_options(awareness_kill_sound_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_kill_sound COMMAND awareness_kill_sound_tests)
  add_test(NAME awareness_audio_device COMMAND awareness_kill_sound_tests --device)
  set_tests_properties(awareness_audio_device PROPERTIES TIMEOUT 30 SKIP_RETURN_CODE 77)
  set_tests_properties(awareness_kill_sound PROPERTIES TIMEOUT 20)
  add_executable(awareness_cs2_model_draw_tests tests/cs2_model_draw_tests.cpp)
  target_link_libraries(awareness_cs2_model_draw_tests PRIVATE awareness_math)
  target_include_directories(awareness_cs2_model_draw_tests PRIVATE src)
  target_compile_definitions(awareness_cs2_model_draw_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_cs2_model_draw_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_cs2_model_draw COMMAND awareness_cs2_model_draw_tests)
  add_executable(awareness_menu_media_tests tests/menu_media_tests.cpp src/model_preview.cpp "${CMAKE_CURRENT_BINARY_DIR}/overlay_resources.rc")
  target_link_libraries(awareness_menu_media_tests PRIVATE awareness_math d3d11 d3dcompiler windowscodecs ole32 comdlg32)
  target_include_directories(awareness_menu_media_tests PRIVATE src)
  target_compile_definitions(awareness_menu_media_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  target_compile_options(awareness_menu_media_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_menu_media COMMAND awareness_menu_media_tests)
  add_executable(awareness_effects_tests tests/effects_tests.cpp src/entity_effects.cpp)
  target_link_libraries(awareness_effects_tests PRIVATE awareness_math d3d11 d3dcompiler)
  target_include_directories(awareness_effects_tests PRIVATE src)
  target_compile_definitions(awareness_effects_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_effects_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_effects COMMAND awareness_effects_tests)
  add_executable(awareness_cs2_glow_tests tests/cs2_glow_tests.cpp src/cs2_source.cpp)
  target_link_libraries(awareness_cs2_glow_tests PRIVATE awareness_math psapi)
  target_include_directories(awareness_cs2_glow_tests PRIVATE src)
  target_compile_definitions(awareness_cs2_glow_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_cs2_glow_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_cs2_glow COMMAND awareness_cs2_glow_tests)
  add_executable(awareness_cs2_camera_tests tests/cs2_camera_tests.cpp src/cs2_source.cpp)
  target_link_libraries(awareness_cs2_camera_tests PRIVATE awareness_math psapi)
  target_include_directories(awareness_cs2_camera_tests PRIVATE src)
  target_compile_definitions(awareness_cs2_camera_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_cs2_camera_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_cs2_camera COMMAND awareness_cs2_camera_tests)
  add_executable(awareness_local_memory_tests tests/local_memory_tests.cpp)
  target_link_libraries(awareness_local_memory_tests PRIVATE awareness_math)
  target_include_directories(awareness_local_memory_tests PRIVATE src)
  add_test(NAME awareness_local_memory COMMAND awareness_local_memory_tests)
  add_executable(awareness_weapon_tests tests/weapon_tests.cpp)
  target_include_directories(awareness_weapon_tests PRIVATE src)
  add_test(NAME awareness_weapons COMMAND awareness_weapon_tests)
  add_executable(awareness_build_verification_tests tests/build_verification_tests.cpp)
  target_link_libraries(awareness_build_verification_tests PRIVATE awareness_math)
  target_include_directories(awareness_build_verification_tests PRIVATE src)
  add_test(NAME awareness_build_verification COMMAND awareness_build_verification_tests)
  add_executable(awareness_settings_tests tests/settings_tests.cpp src/settings.cpp)
  target_link_libraries(awareness_settings_tests PRIVATE awareness_math)
  target_include_directories(awareness_settings_tests PRIVATE src)
  add_test(NAME awareness_settings COMMAND awareness_settings_tests)
  add_executable(awareness_input_tests tests/input_tests.cpp src/window_bridge.cpp)
  target_link_libraries(awareness_input_tests PRIVATE awareness_math awareness_imgui user32)
  target_include_directories(awareness_input_tests PRIVATE src)
  add_test(NAME awareness_input COMMAND awareness_input_tests)
  add_executable(awareness_api_tests tests/api_tests.cpp)
  target_include_directories(awareness_api_tests PRIVATE src)
  target_link_libraries(awareness_api_tests PRIVATE awareness_math EntityAwarenessOverlay)
  add_test(NAME awareness_api COMMAND awareness_api_tests)
  add_executable(awareness_cs2_live_probe tests/cs2_live_probe.cpp)
  target_link_libraries(awareness_cs2_live_probe PRIVATE awareness_math psapi)
  target_include_directories(awareness_cs2_live_probe PRIVATE src)
  add_library(AwarenessScanFixture SHARED tests/scan_fixture.cpp)
  target_compile_definitions(AwarenessScanFixture PRIVATE SCAN_FIXTURE_EXPORTS)
  target_link_options(AwarenessScanFixture PRIVATE "/SECTION:.awtest,ERW")
  target_link_libraries(AwarenessScanFixture PRIVATE awareness_math)
  add_executable(awareness_scanner_integration_tests tests/scanner_integration_tests.cpp)
  target_link_libraries(awareness_scanner_integration_tests PRIVATE awareness_math EntityAwarenessOverlay AwarenessScanFixture)
  add_test(NAME awareness_scanner_integration COMMAND awareness_scanner_integration_tests)
endif()
foreach(target awareness_imgui EntityAwarenessOverlay ObserverDemo AwarenessScanFixture awareness_scanner_integration_tests awareness_cs2_live_probe awareness_api_tests awareness_build_verification_tests awareness_settings_tests awareness_input_tests awareness_local_memory_tests awareness_weapon_tests)
  if(NOT TARGET ${target})
    continue()
  endif()
  target_compile_definitions(${target} PRIVATE UNICODE _UNICODE WIN32_LEAN_AND_MEAN NOMINMAX)
endforeach()
if(MSVC)
  if(TARGET awareness_camera_tracking_tests)
    target_compile_options(awareness_camera_tracking_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  endif()
  foreach(target EntityAwarenessOverlay ObserverDemo awareness_math_tests awareness_pattern_tests awareness_cs2_reader_tests AwarenessScanFixture awareness_scanner_integration_tests awareness_cs2_live_probe awareness_api_tests awareness_build_verification_tests awareness_settings_tests awareness_input_tests awareness_local_memory_tests awareness_weapon_tests)
    if(TARGET ${target})
      target_compile_options(${target} PRIVATE "$<$<COMPILE_LANGUAGE:C,CXX>:/W4;/permissive-;/EHsc;/utf-8>")
    endif()
  endforeach()
endif()
if(BUILD_TESTING)
  find_program(AWARENESS_POWERSHELL NAMES powershell pwsh REQUIRED)
  add_test(NAME awareness_offsets COMMAND "${AWARENESS_POWERSHELL}" -NoProfile -NonInteractive -ExecutionPolicy Bypass
    -File "${CMAKE_CURRENT_SOURCE_DIR}/scripts/generate-offsets.ps1" -Check)
  add_test(NAME awareness_build_support COMMAND "${AWARENESS_POWERSHELL}" -NoProfile -NonInteractive -ExecutionPolicy Bypass
    -File "${CMAKE_CURRENT_SOURCE_DIR}/tests/build_support_tests.ps1" -TestDirectory "${CMAKE_CURRENT_BINARY_DIR}/script-tests")
  add_test(NAME awareness_offset_generation COMMAND "${AWARENESS_POWERSHELL}" -NoProfile -NonInteractive -ExecutionPolicy Bypass
    -File "${CMAKE_CURRENT_SOURCE_DIR}/tests/offset_generation_tests.ps1" -TestDirectory "${CMAKE_CURRENT_BINARY_DIR}/script-tests")
  add_test(NAME awareness_dx11_smoke COMMAND ObserverDemo --smoke)
  set_tests_properties(awareness_dx11_smoke PROPERTIES TIMEOUT 45)
  add_test(NAME awareness_camera_smoke COMMAND ObserverDemo --camera-smoke)
  set_tests_properties(awareness_camera_smoke PROPERTIES TIMEOUT 45)
endif()
if(BUILD_TESTING AND WIN32)
  foreach(test awareness_settings awareness_dx11_smoke awareness_camera_smoke)
    set_tests_properties(${test} PROPERTIES
      ENVIRONMENT "VORTEX_DATA_DIR=${CMAKE_CURRENT_BINARY_DIR}/isolated-test-profile/${test}")
  endforeach()
endif()

if(BUILD_TESTING AND WIN32)
  add_executable(awareness_runtime_tests tests/runtime_tests.cpp)
  target_include_directories(awareness_runtime_tests PRIVATE src)
  target_link_libraries(awareness_runtime_tests PRIVATE awareness_math user32)
  target_compile_definitions(awareness_runtime_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  add_test(NAME awareness_runtime COMMAND awareness_runtime_tests)
  set_tests_properties(awareness_runtime PROPERTIES TIMEOUT 15)
  add_executable(vortex_profile_tool scripts/profile-tool.cpp src/settings.cpp)
  target_include_directories(vortex_profile_tool PRIVATE src)
  target_link_libraries(vortex_profile_tool PRIVATE awareness_math)
  target_compile_definitions(vortex_profile_tool PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
endif()

if(BUILD_TESTING AND WIN32)
  add_executable(awareness_recoil_reader_tests tests/recoil_reader_tests.cpp)
  target_include_directories(awareness_recoil_reader_tests PRIVATE src)
  target_link_libraries(awareness_recoil_reader_tests PRIVATE awareness_math)
  target_compile_definitions(awareness_recoil_reader_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  add_test(NAME awareness_recoil_reader COMMAND awareness_recoil_reader_tests)
endif()

if(BUILD_TESTING AND WIN32)
  add_executable(awareness_session_tests tests/session_tests.cpp)
  target_include_directories(awareness_session_tests PRIVATE src)
  target_link_libraries(awareness_session_tests PRIVATE awareness_math)
  target_compile_definitions(awareness_session_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  add_test(NAME awareness_sessions COMMAND awareness_session_tests)
endif()

if(BUILD_TESTING AND WIN32)
  add_executable(awareness_world_features_tests tests/feature319_tests.cpp)
  target_include_directories(awareness_world_features_tests PRIVATE src)
  target_link_libraries(awareness_world_features_tests PRIVATE awareness_math awareness_imgui)
  target_compile_definitions(awareness_world_features_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_world_features_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_world_features COMMAND awareness_world_features_tests)
endif()

if(WIN32 AND MSVC AND BUILD_TESTING)
  add_executable(awareness_assist_tests tests/assist_tests.cpp)
  target_link_libraries(awareness_assist_tests PRIVATE awareness_math)
  target_include_directories(awareness_assist_tests PRIVATE src)
  target_compile_definitions(awareness_assist_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  target_compile_options(awareness_assist_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_assists COMMAND awareness_assist_tests)
  add_executable(awareness_visual_style_tests tests/visual_style_tests.cpp)
  target_link_libraries(awareness_visual_style_tests PRIVATE awareness_math awareness_imgui)
  target_include_directories(awareness_visual_style_tests PRIVATE src)
  target_compile_definitions(awareness_visual_style_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  target_compile_options(awareness_visual_style_tests PRIVATE /W4 /permissive- /EHsc /utf-8)
  add_test(NAME awareness_visual_styles COMMAND awareness_visual_style_tests)
endif()

if(BUILD_TESTING AND WIN32 AND MSVC)
  foreach(kind profile_store profile_io)
    add_executable(awareness_${kind}_tests tests/${kind}_tests.cpp src/settings.cpp)
    target_link_libraries(awareness_${kind}_tests PRIVATE awareness_math comdlg32 user32)
    target_include_directories(awareness_${kind}_tests PRIVATE src)
    target_compile_definitions(awareness_${kind}_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
    target_compile_options(awareness_${kind}_tests PRIVATE /W4 /EHsc /permissive- /utf-8)
    add_test(NAME awareness_${kind} COMMAND awareness_${kind}_tests)
    set_tests_properties(awareness_${kind} PROPERTIES TIMEOUT 20
      ENVIRONMENT "VORTEX_DATA_DIR=${CMAKE_CURRENT_BINARY_DIR}/isolated-test-profile/${kind}")
  endforeach()
endif()

if(BUILD_TESTING AND WIN32)
  add_executable(awareness_lineup_tests tests/lineup_tests.cpp src/grenade_lineups.cpp)
  target_include_directories(awareness_lineup_tests PRIVATE src)
  target_link_libraries(awareness_lineup_tests PRIVATE awareness_math awareness_imgui ole32 shell32)
  target_compile_definitions(awareness_lineup_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
  target_compile_options(awareness_lineup_tests PRIVATE /W4 /EHsc /utf-8)
  add_test(NAME awareness_lineups COMMAND awareness_lineup_tests)
endif()

if(BUILD_TESTING AND WIN32)
  add_executable(awareness_deferred_log_tests tests/deferred_log_tests.cpp)
  target_include_directories(awareness_deferred_log_tests PRIVATE src)
  add_test(NAME awareness_deferred_log COMMAND awareness_deferred_log_tests)
  set_tests_properties(awareness_deferred_log PROPERTIES TIMEOUT 10)
endif()

if(BUILD_TESTING AND WIN32)
  add_executable(awareness_camera_visual_tests tests/camera_visual_tests.cpp)
  target_include_directories(awareness_camera_visual_tests PRIVATE src)
  target_link_libraries(awareness_camera_visual_tests PRIVATE awareness_math)
  target_compile_definitions(awareness_camera_visual_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_camera_visual_tests PRIVATE /W4 /EHsc /utf-8)
  add_test(NAME awareness_camera_visuals COMMAND awareness_camera_visual_tests)
  set_tests_properties(awareness_camera_visuals PROPERTIES TIMEOUT 15)
endif()

if(BUILD_TESTING AND WIN32 AND MSVC)
  add_executable(awareness_native_model_mask_tests tests/native_model_mask_tests.cpp tests/native_draw_fixture.asm src/native_model_mask.cpp src/native_draw_shim.asm src/scene_depth_capture.cpp)
  target_include_directories(awareness_native_model_mask_tests PRIVATE src)
  target_link_libraries(awareness_native_model_mask_tests PRIVATE awareness_math minhook d3d11 d3dcompiler)
  target_compile_definitions(awareness_native_model_mask_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_native_model_mask_tests PRIVATE "$<$<COMPILE_LANGUAGE:CXX>:/W4;/permissive-;/EHsc;/utf-8>")
  add_test(NAME awareness_native_model_mask COMMAND awareness_native_model_mask_tests)
  set_tests_properties(awareness_native_model_mask PROPERTIES TIMEOUT 30)
  add_executable(awareness_weather_tests tests/weather_tests.cpp)
  target_include_directories(awareness_weather_tests PRIVATE src)
  target_link_libraries(awareness_weather_tests PRIVATE awareness_math)
  target_compile_definitions(awareness_weather_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  add_test(NAME awareness_weather COMMAND awareness_weather_tests)
endif()

if(BUILD_TESTING AND WIN32 AND MSVC)
  add_executable(awareness_cosmetics_tests tests/cosmetics_tests.cpp src/cosmetics_native.cpp src/economy_catalog.cpp)
  target_include_directories(awareness_cosmetics_tests PRIVATE src)
  target_link_libraries(awareness_cosmetics_tests PRIVATE awareness_math psapi)
  target_compile_definitions(awareness_cosmetics_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_cosmetics_tests PRIVATE /W4 /EHsc /permissive- /utf-8)
  add_test(NAME awareness_cosmetics COMMAND awareness_cosmetics_tests)
  set_tests_properties(awareness_cosmetics PROPERTIES TIMEOUT 20)
endif()

if(BUILD_TESTING AND WIN32 AND MSVC)
  add_executable(awareness_scoreboard_tests tests/scoreboard_tests.cpp src/scoreboard.cpp)
  target_include_directories(awareness_scoreboard_tests PRIVATE src)
  target_link_libraries(awareness_scoreboard_tests PRIVATE awareness_math)
  target_compile_definitions(awareness_scoreboard_tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_compile_options(awareness_scoreboard_tests PRIVATE /W4 /EHsc /permissive- /utf-8)
  add_test(NAME awareness_scoreboard COMMAND awareness_scoreboard_tests)
  find_program(VORTEX_NODE_EXECUTABLE NAMES node)
  if(VORTEX_NODE_EXECUTABLE)
    add_test(NAME awareness_scoreboard_ui COMMAND "${VORTEX_NODE_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/scoreboard_ui_tests.cjs")
  endif()
endif()
